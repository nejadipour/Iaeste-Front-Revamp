import React from 'react';
import {Image} from "antd";

const IncomingStudentsIcon = ({style}) => (
    <Image style={style} preview={false} alt={""} src={"/assets/icons/incoming_students.svg"}/>
);

export default IncomingStudentsIcon;
