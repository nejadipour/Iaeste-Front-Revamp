import React from 'react';
import {Image} from "antd";

const MarketingIcon = ({style}) => (
    <Image style={style} preview={false} alt={""} src={"/assets/icons/marketing.svg"}/>
);

export default MarketingIcon;
