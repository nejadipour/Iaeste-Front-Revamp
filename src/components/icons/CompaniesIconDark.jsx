import React from 'react';
import {Image} from "antd";

const CompaniesIconDark = ({style}) => (
    <Image style={style} preview={false} alt={""} src={"/assets/icons/companies_dark.svg"}/>
);

export default CompaniesIconDark;
