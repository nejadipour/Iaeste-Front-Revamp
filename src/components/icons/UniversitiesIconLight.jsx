import React from 'react';
import {Image} from "antd";

const UniversitiesIconLight = ({style}) => (
    <Image style={style} preview={false} alt={""} src={"/assets/icons/universities_light.svg"}/>
);

export default UniversitiesIconLight;
