import React from 'react';
import {Image} from "antd";

const StudentsIconLight = ({style}) => (
    <Image style={style} preview={false} alt={""} src={"/assets/icons/students_light.svg"}/>
);

export default StudentsIconLight;
