import React from 'react';
import {Image} from "antd";

const OutgoingStudentsIcon = ({style}) => (
    <Image style={style} preview={false} alt={""} src={"/assets/icons/outgoing_students.svg"}/>
);

export default OutgoingStudentsIcon;
