import React from 'react';
import {Image} from "antd";

const LocalIcon = ({style}) => (
    <Image style={style} preview={false} alt={""} src={"/assets/icons/local.svg"}/>
);

export default LocalIcon;
