import React from 'react';
import {Image} from "antd";

const StudentsIconDark = ({style}) => (
    <Image style={style} preview={false} alt={""} src={"/assets/icons/students_dark.svg"}/>
);

export default StudentsIconDark;
