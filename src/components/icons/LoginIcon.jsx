import React from 'react';
import {Image} from "antd";

const LoginIcon = ({style}) => (
    <Image style={style} preview={false} alt={""} src={"/assets/icons/login.svg"}/>
);

export default LoginIcon;
