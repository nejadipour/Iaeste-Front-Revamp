import React from 'react';
import {Image} from "antd";

const UniversitiesIconDark = ({style}) => (
    <Image style={style} preview={false} alt={""} src={"/assets/icons/universities_dark.svg"}/>
);

export default UniversitiesIconDark;
