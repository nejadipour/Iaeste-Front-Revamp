import React from 'react';
import {Image} from "antd";

const UniversityIcon = ({style}) => (
    <Image style={style} preview={false} alt={""} src={"/assets/icons/university.svg"}/>
);

export default UniversityIcon;
