import React from 'react';
import {Image} from "antd";

const CalendarIcon = ({style}) => (
    <Image style={style} preview={false} alt={""} src={"/assets/icons/calendar.svg"}/>
);

export default CalendarIcon;
