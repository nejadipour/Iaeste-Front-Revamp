import React from 'react';
import {Image} from "antd";

const CalendarIcon = ({style}) => (
    <Image style={style} preview={false} alt={""} src={"/assets/icons/calendar_event.svg"}/>
);

export default CalendarIcon;
