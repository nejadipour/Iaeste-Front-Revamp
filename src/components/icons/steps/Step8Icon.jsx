import React from 'react';
import {Image} from "antd";

const Step8Icon = ({style}) => (
    <Image style={style} preview={false} alt={""} src={"/assets/icons/step8.svg"}/>
);

export default Step8Icon;
