import React from 'react';
import {Image} from "antd";

const Step5Icon = ({style}) => (
    <Image style={style} preview={false} alt={""} src={"/assets/icons/step5.svg"}/>
);

export default Step5Icon;
