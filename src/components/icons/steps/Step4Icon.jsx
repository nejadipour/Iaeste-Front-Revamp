import React from 'react';
import {Image} from "antd";

const Step4Icon = ({style}) => (
    <Image style={style} preview={false} alt={""} src={"/assets/icons/step4.svg"}/>
);

export default Step4Icon;
