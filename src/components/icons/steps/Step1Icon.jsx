import React from 'react';
import {Image} from "antd";

const Step1Icon = ({style}) => (
    <Image style={style} preview={false} alt={""} src={"/assets/icons/step1.svg"}/>
);

export default Step1Icon;
