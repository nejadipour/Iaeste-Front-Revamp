import React from 'react';
import {Image} from "antd";

const Step2Icon = ({style}) => (
    <Image style={style} preview={false} alt={""} src={"/assets/icons/step2.svg"}/>
);

export default Step2Icon;
