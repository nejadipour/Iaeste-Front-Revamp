import React from 'react';
import {Image} from "antd";

const FieldIcon = ({style}) => (
    <Image style={style} preview={false} alt={""} src={"/assets/icons/field.svg"}/>
);

export default FieldIcon;
