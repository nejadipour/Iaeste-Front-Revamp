import React from 'react';
import {Image} from "antd";

const TelegramIcon = ({style}) => (
    <Image style={style} preview={false} alt={""} src={"/assets/icons/telegram.svg"}/>
);

export default TelegramIcon;
