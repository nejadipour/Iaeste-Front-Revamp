import React from 'react';
import {Image} from "antd";

const LocationIcon = ({style}) => (
    <Image style={style} preview={false} alt={""} src={"/assets/icons/location_event.svg"}/>
);

export default LocationIcon;
